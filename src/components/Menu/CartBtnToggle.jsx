'use client'

const CartBtnToggle = () => {
    const handleCartBtn = ()=> {

    }
    return (
        <button onClick={handleCartBtn} className="flex-1 bg-orange-500 text-white py-2 rounded-xl hover:bg-orange-600 transition">
            Add Cart
        </button>
    );
};

export default CartBtnToggle;